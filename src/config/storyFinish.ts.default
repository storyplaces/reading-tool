// This config file contains the content for the Story Finish page, which is configured on a per-site basis.

var storyFinish: String = `
    <p>
        The story is now finished, thank you for reading.
    </p>
`;

export default storyFinish;